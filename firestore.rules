rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone with the site link can view the live schedule.
    // Only signed-in organizers (created in Firebase Auth) can edit it.
    match /events/{eventId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
